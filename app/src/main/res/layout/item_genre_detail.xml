<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <variable
            name="genre"
            type="com.example.hcm_102_0006.android_project_m.data.model.Genres"/>
        <variable
            name="itemView"
            type="com.example.hcm_102_0006.android_project_m.ui.moviedetail.MovieDetailViewModel"/>
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/margin_between_Item"
        android:onClick="@{()->itemView.onResultGenres(genre)}">>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{genre.getName}"/>
    </LinearLayout>
</layout>
